name: Sync Match Results

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call sync endpoint
        run: |
          curl -X GET \
            -H "Authorization: Bearer mundial2026secret" \
            -f -s -S \
            https://quiniela-mundial-2026-theta.vercel.app/api/sync-results
